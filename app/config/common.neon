includes:
	- local.neon

application:
	mapping:
		*: [App, Modules\*, Endpoints\*]

session:
	expiration: 14 days

di:
	export:
		parameters: no
		tags: no

services:
	router: App\Router\RouterFactory::createRouter